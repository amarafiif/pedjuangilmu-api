{
	"version": 2,
	"builds": [
		{
			"src": "index.js",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/api-docs/(.*)",
			"dest": "/api-docs/$1"
		},
		{
			"src": "/api/v1/(.*)",
			"dest": "/$1"
		},
		{
			"handle": "filesystem",
			"src": "/swagger/(.*)",
			"dest": "/$1"
		},
		{
			"src": ".*",
			"dest": "index.js"
		}
	],
	"env": {
		"NODE_ENV": "production"
	}
}
